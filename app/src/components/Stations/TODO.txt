- Have an key event listener that can be hooked onto from anywhere
- Make it be reactive so update logic isn't complicated 
- Provide a listener/watcher that subscribes to the pressed key
- Metrics could be retrieved via a map
- Since only one instance of this is ever really active we don't need to make it a store
- Leave it as a composable 